[package]
name = "jayson_test"
version = "0.0.0"
edition = "2018"
publish = false

[lib]
path = "test.rs"

[dependencies]
jayson = { path = "../..", default-features = false }

[features]
default = ["std"]
std = ["jayson/std"]
